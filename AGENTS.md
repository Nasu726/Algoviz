# AlgoViz

アルゴリズムの動きを1手ずつ見せる静的 Web ページ。ロジックは C++ → WASM、描画は React + PixiJS。

## 最初に問うこと: 責任範囲の線引き

**AlgoViz が保証するのは「アルゴリズムが動く様子を1手ずつ見せること」だけ。**
それ以外は外部の資料 (技術ブログ、教科書、Wikipedia) が既に担っている。

何かを足す前に必ず問う: **これは AlgoViz が保証すべき体験の範囲内か、外部が担う領域か。**
線を引かないと実装が膨らみ、本来見るべき点を見落とす。

- 範囲外の例: アルゴリズムの解説文、用語の定義、計算量の説明。
  ビジュアライザ一覧に「始点から近い順に広がる」のような説明は**書かない**
- 範囲内の例: このページの操作方法、画面の色や数字が何を指すか、
  そのアルゴリズム固有の「動きを見ないと分からないこと」

**文章に限らない。** UI 要素・設定項目・表示のどれにも同じ問いを当てる。
迷ったら足さない方に倒す。

詳しくは README の「設計方針」。

## 書き方

- 説明を足しすぎない。コメントもドキュメントも、必要な分だけ
- 1ページ1アルゴリズム。モード切り替えのプルダウンは置かない
- 分類ごとに決まるものは、利用者に選ばせずに固定する。
  頂点の表示名がその例 (グラフ系は `0, 1, 2`、オートマトンは `q₀, q₁, q₂`、
  木は節点の値)。グラフの頂点を q₁ と書くことも、オートマトンの状態を
  0 と書くこともまず無い。C++ 側の `labelMode` が唯一の情報源
- アルゴリズムの論理は C++、見た目は JS。迷ったら
  「別の描画方法に差し替えても必要か」で決める。必要なら C++ 側
- デバッグ用の表示は `import.meta.env.DEV` で囲み、本番バンドルから落とす

## 忘れやすい手順

- **C++ を変えたら WASM を再ビルドしてコミットする。** Cloudflare Pages の
  ビルド環境に emscripten は無いので、`public/wasm/` の成果物はリポジトリに含める

  ```bash
  npm run build:wasm
  npm run test:smoke   # 出荷する core.js への疎通。再ビルド漏れを検出する
  ```

- **`emcc` ではなく `em++`。** C++ をリンクするので `emcc` だと
  `operator new/delete` が未定義になる
- **emscripten のバージョンは `emscripten/emsdk:5.0.1` に固定。**
  `latest` にするとローカルと CI で別のコンパイラが動き、出荷する WASM が再現しない
- リファクタの前にテストを書く。`step()` と `runToEnd()` の統合では
  「両者の結果が一致する」テストが安全網になった
- **ビジュアライザを足したら3か所を更新する。** どれも忘れやすい
  - `src/pages/Menu.tsx` の一覧 (これが無いと辿り着けない)
  - `README.md` の「現在利用可能なビジュアライザ」
  - `visualize_plan.md` の該当項目にチェック

## PR の出し方

- **ベースは必ず `main`。** 別の PR のブランチを指さない。
  マージの順番が縛られるうえ、**ベースブランチが削除されると GitHub が
  PR を自動で閉じる。** 閉じた PR はベースが消えていると再オープンもできず、
  リベースして出し直すことになる
- 領域が重ならない作業は `main` ベースで並列に出す。
  作業を分けたいだけならブランチを分ければ足りる
- こまめにコミットする。問題が起きたときに手前へ戻せるようにしておく

## テスト

ネイティブの C++ コンパイラは使わず、本番と同じ em++ で WASM にビルドして Node で実行する。

```bash
npm test --silent
```

成功時は結果の1行だけ。失敗したテストだけが名前と詳細を出す。
**出力があれば何か壊れている。** 進行を見たいときは `--verbose`。

新しいテストも同じ方針で書く。成功時に何も出さないこと。

## 先送りにしている判断

- **オートマトンは DFA だけ。** NFA は現在状態が集合になり ε-閉包も要るので、
  実行部も UI も別物になる。BFS / DFS を分けたのと同じようにページを分ける。
  subset construction も「実際のアルゴリズム」として独立したページにする

## 配置アルゴリズムの分担

`ILayout` の実装で分ける。**向きは配置が決めるもので、利用者に選ばせる設定ではない。**

- `GeneralGraphLayout` … 一般グラフ。力学モデルで、横長に伸ばす
- `TreeLayout` … 木。Reingold-Tilford で、常に上から下

森を並べるのに一般グラフの凸包パッキングは要らない。**木の外形は長方形なので、
横にずらすだけで重ならない。** `GeneralGraphLayout` からパッキングを切り出す話は、
木のためには不要と分かっている。

木の左右の並びは**辺の3列目**で指定する。辺は木が育った順に増えるので、
追加順のままだと二分探索木で小さい値が右に来ることがある。

## 円形配置に切り替える条件

`GeneralGraphLayout::CIRCULAR_MIN_DENSITY`。**密度 D = 2E / V(V-1) がこれ以上なら円形**。
完全グラフなら D = 1。

**平均次数ではなく密度で切る。** 平均次数で切ると、頂点数が増えるほど疎なグラフまで
円形に落ちる。実際に見比べたところ、力学モデルが円形に負けるのは
「完全に近い」ときだけだった。

| 形 | 密度 | どちらが読めるか |
|---|---|---|
| 星 / 木 / 格子 / 車輪 | 0.06〜0.20 | 力学 (格子は交差ゼロで描ける) |
| かたまりが3つあるグラフ | 0.31 | **力学** (3つの塊を分離して描く) |
| ランダム 平均次数8 | 0.35 | 力学 |
| 完全に近い / 完全グラフ | 0.85〜1.0 | **円形** (多角形として読める) |

以前あったハブ補正 `min(1, C/maxK)` は**符号が逆で、外した**。ハブがあるほど円形に
寄せていたが、ハブは力学モデルにとって有利で円形にとっては最悪の構造。
星グラフ (n=20) が円形になり、19本の弦が円を横切っていた。

**性能ガードではない。** V=50 の生成にかかる時間は平均次数2で 60ms、4で 40ms、
円形は 3ms。重い経路の費用は密度ではなく頂点数で決まり、疎なグラフは元から
同じ経路を通っていた。

**逆 Cuthill-McKee の並び順は、もうほとんど効かない。** 円形が完全に近いグラフ専用に
なったので、どう並べても弦はほぼ全部ある。消してよいが、害も無いので残してある。

